# Addendum 2026-09-04 — Batch One Close-out and Scheduling

Session record: the skeleton/transport child is archived with all eleven ACs
passing — AC3 was closed by a live Claude Code dual-transport test (evidence in
its `research/step9-integration-verification.md`). Parent progress is 2/5 done.
No new implementation work was started in this session.

## Where batch one stands

Archived: `08-29-pippin-architecture-reuse-safety-hardening`,
`08-17-pippin-skeleton-transport`.

Remaining, all in planning, all prerequisites satisfied:

- `08-17-pippin-reminders-crud` (P1) — first real module; writes, two-phase
  delete, iCloud correctness; reproduces and fixes the known Reminders
  flakiness.
- `08-24-pippin-server-lifecycle-toggle` (P1) — required before production
  use; small surface, no Apple-data dependencies.
- `08-17-pippin-mail-read-search` (P2) — proves routing/degradation; the
  highest validation cost of the three (private schema drift).

## Recommended order

1. **Reminders CRUD next** — P1, highest user value, fully unblocked.
2. **Screen Time archival (Batch 2) in parallel, or first** — time-sensitive.
   `knowledgeC.db` retains roughly four weeks (verified 2026-07-21 → 08-17 in
   the parent PRD). The manual backup stops at 2026-08-17; days from 08-18
   onward exist only in the live rolling database and the earliest of them
   starts expiring around mid-September. Every day without the daily archive
   job is a permanently lost day. Needs its own task created before work
   starts.
3. **Lifecycle toggle** before any production use; a good short task to slot
   in whenever a gap appears.
4. **Mail read/search** last of batch one (P2); independent of Reminders.

This addendum records scheduling judgment only; it changes nothing in the
parent PRD's decided positions or batch plan.
